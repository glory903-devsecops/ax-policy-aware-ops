[project]
name = "ax-decision-fabric"
version = "0.1.0"
description = "Policy-aware operational decision layer for AX-driven IT operations"
requires-python = ">=3.12"

[tool.black]
line-length = 88

[tool.pytest.ini_options]
pythonpath = ["."]
